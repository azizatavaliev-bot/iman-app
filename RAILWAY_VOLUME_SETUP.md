# 🚨 КРИТИЧНО: Настройка Railway Volume для сохранения данных

## ⚠️ ПРОБЛЕМА

**Данные пользователей теряются при каждом деплое!**

При каждом обновлении кода Railway пересоздаёт контейнер и удаляет все файлы, включая базу данных SQLite (`iman.db`).

**Результат:**
- ❌ Все пользователи исчезают
- ❌ Вся аналитика пропадает
- ❌ История действий теряется

---

## ✅ РЕШЕНИЕ: Railway Volume

Railway предоставляет **постоянное хранилище (Volume)** которое сохраняется между деплоями.

---

## 📋 Инструкция по настройке (ОБЯЗАТЕЛЬНО!)

### Шаг 1: Откройте Railway Dashboard

1. Перейдите на https://railway.app/dashboard
2. Найдите проект **iman-app-production**
3. Кликните на него

### Шаг 2: Создайте Volume

1. В левой панели нажмите на сервис (название проекта)
2. Перейдите во вкладку **"Settings"** (Настройки)
3. Прокрутите вниз до секции **"Volumes"**
4. Нажмите **"+ New Volume"**

### Шаг 3: Настройте параметры Volume

Введите следующие параметры:

- **Name:** `iman-data`
- **Mount Path:** `/data`
- **Size:** `1 GB` (можно увеличить потом)

Нажмите **"Create Volume"**

### Шаг 4: Перезапустите сервис

1. Вернитесь во вкладку **"Deployments"**
2. Нажмите **"Redeploy"** на последнем деплое
3. Подождите 2-3 минуты

---

## 🔍 Проверка, что Volume работает

### Метод 1: Через Railway Dashboard

1. Откройте Railway Dashboard → ваш проект
2. Перейдите во вкладку **"Settings"** → секция **"Volumes"**
3. Должен быть виден Volume **iman-data** со статусом **"Active"**
4. После деплоя проверьте **"Usage"** — должен показывать размер базы данных (не 0)

### Метод 2: Через приложение

1. Откройте IMAN App в Telegram
2. Зарегистрируйтесь / войдите
3. Отметьте намаз или сделайте любое действие
4. Откройте админ-панель → проверьте, что вы там видны
5. **ВАЖНО:** Сделайте redeploy на Railway (Deployments → Redeploy)
6. Подождите 2-3 минуты
7. Снова откройте админ-панель
8. ✅ **Если ваши данные сохранились** — Volume работает!
9. ❌ **Если данные пропали** — Volume не настроен правильно

---

## 📂 Что хранится в `/data`

После настройки Volume в `/data` будут храниться:

- **`iman.db`** — база данных SQLite со всеми пользователями
- **`iman.db-wal`** — WAL файл (Write-Ahead Log)
- **`iman.db-shm`** — Shared memory файл
- **`subscribers.json`** — список подписчиков Telegram бота

---

## 🚨 Если Volume НЕ настроен

**Что происходит при каждом деплое:**

```
1. Railway создаёт новый контейнер
2. Копирует код приложения
3. Запускает npm run build
4. Запускает node server.js
5. Создаётся НОВАЯ ПУСТАЯ база данных в /data
6. ВСЕ СТАРЫЕ ДАННЫЕ УДАЛЯЮТСЯ ❌
```

**Что происходит С Volume:**

```
1. Railway создаёт новый контейнер
2. Копирует код приложения
3. ПОДКЛЮЧАЕТ Volume к /data ✅
4. Запускает node server.js
5. Использует СУЩЕСТВУЮЩУЮ базу данных
6. Все данные СОХРАНЯЮТСЯ ✅
```

---

## 💾 Размер Volume

**Рекомендации:**

| Пользователей | Размер БД | Volume Size |
|---------------|-----------|-------------|
| 1,000 | ~10 MB | 500 MB |
| 10,000 | ~100 MB | 1 GB |
| 50,000 | ~500 MB | 2 GB |
| 100,000 | ~1 GB | 5 GB |

**Текущий план Railway:**
- **Hobby ($5/мес):** 1 GB Volume включено
- **Pro ($20/мес):** 5 GB Volume
- **Team ($100/мес):** 50 GB Volume

---

## 🔄 Что делать, если данные уже потерялись

**К сожалению, старые данные восстановить нельзя.** Но вот что можно сделать:

### Вариант 1: Подождать новых пользователей
1. Настройте Volume (инструкция выше)
2. Новые пользователи начнут регистрироваться
3. Их данные уже НЕ пропадут

### Вариант 2: Импорт из бэкапа (если есть)
Если у вас есть бэкап `iman.db`:
1. Откройте Railway Dashboard → ваш проект
2. Перейдите во вкладку **"Shell"** (терминал)
3. Нажмите **"Open Shell"**
4. Загрузите файл через SCP или используйте Railway CLI

---

## 📊 Мониторинг Volume

### Через Railway Dashboard:
1. Settings → Volumes → iman-data
2. Смотрите **"Usage"** (использование)
3. Если использование близко к лимиту — увеличьте Size

### Через Shell (для продвинутых):
```bash
# Открыть Railway Shell
railway shell

# Проверить размер базы
du -h /data/iman.db

# Проверить все файлы
ls -lh /data/
```

---

## ✅ Чеклист перед запуском

- [ ] Railway Volume создан (Name: `iman-data`, Path: `/data`)
- [ ] Volume в статусе "Active"
- [ ] Код обновлён (использует `/data` для Railway)
- [ ] Сделан redeploy после создания Volume
- [ ] Проверено: данные сохраняются после redeploy

---

## 🆘 Если что-то не работает

1. **Volume создан, но данные всё равно пропадают:**
   - Проверьте, что Mount Path = `/data` (не `/app/data`)
   - Сделайте полный redeploy (не просто restart)

2. **Не можете создать Volume:**
   - Проверьте, что Railway Plan поддерживает Volumes (Hobby и выше)
   - Free tier НЕ поддерживает Volumes — нужен платный план

3. **Volume заполнился:**
   - Увеличьте Size в Settings → Volumes
   - Или настройте авто-очистку старых данных (см. SCALABILITY.md)

---

**КРИТИЧНО:** Без Volume все данные теряются при каждом деплое! Настройте Volume ПРЯМО СЕЙЧАС.
