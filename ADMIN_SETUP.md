# Админ-панель IMAN App — Инструкция по настройке

## ✅ Что уже сделано

### 1. Полностью функциональная админ-панель
- 📊 **Live-статистика**: онлайн пользователей (последние 5 мин), активных за день
- 📈 **Аналитика использования**: топ-5 страниц, топ-5 действий
- ⏱️ **Среднее время сессии**: сколько пользователи проводят в приложении
- 👥 **Полная таблица пользователей**: имя, город, очки, намазы, страйк
- 🔍 **Поиск и сортировка**: по любым параметрам
- 📄 **Пагинация**: по 20 пользователей на страницу
- 🔄 **Auto-refresh**: обновление каждые 30 секунд

### 2. Система аналитики (tracking)
- **SQLite таблица `analytics`**: хранит все события пользователей
- **Батчинг событий**: отправка каждые 10 секунд (экономия трафика)
- **Типы событий**:
  - `session_start` / `session_end` — когда пользователь открыл/закрыл приложение
  - `page_view` — какие страницы просматривает
  - `action` — какие действия делает (отметил намаз, включил Коран, и т.д.)

### 3. Безопасность
- ✅ **Двойная проверка**: по Telegram ID (приоритет) + по username (запасной)
- ✅ **Client-side защита**: кнопка админ-панели видна только админам
- ✅ **Server-side защита**: `/api/admin/*` endpoints проверяют authorization headers
- ✅ **Показ Telegram ID**: в Profile показывается ваш ID с кнопкой "Копировать"

### 4. Что отслеживается
- ✅ Отметка намазов (`prayer_marked`)
- ✅ Открытие каждой страницы (`page_view`)
- ✅ Время сессии (от открытия до закрытия)
- ✅ Последняя активность (для определения "онлайн")

---

## 🚀 Как настроить (ВАЖНО!)

### Шаг 1: Получите свой Telegram ID

1. Откройте приложение в Telegram
2. Перейдите в **Профиль** (нижнее меню → "Профиль")
3. Найдите блок **"Ваш Telegram ID"** (показывается фиолетовым цветом)
4. Нажмите **"Копировать"** — ваш ID скопирован в буфер обмена

**Пример:** `123456789` (ваше реальное число будет другим)

---

### Шаг 2: Добавьте ID в код (выберите один способ)

#### Вариант A: Через adminConfig.ts (клиент)

Откройте файл: `src/lib/adminConfig.ts`

```typescript
export const ADMIN_TELEGRAM_IDS = [
  123456789, // ← ВСТАВЬТЕ СЮДА ВАШЕ ЧИСЛО (замените этот пример)
];
```

#### Вариант B: Через server.js (сервер) — РЕКОМЕНДУЕТСЯ

Откройте файл: `server.js` (строка ~68)

```javascript
const ADMIN_TELEGRAM_IDS = [
  123456789, // ← ВСТАВЬТЕ СЮДА ВАШЕ ЧИСЛО
];
```

**⚠️ ВАЖНО:** Добавьте ID в **ОБА** файла для максимальной защиты!

---

### Шаг 3: Пересоберите приложение

```bash
npm run build
```

---

### Шаг 4: Задеплойте на Railway

```bash
# Railway автоматически задеплоит при push в GitHub
# Или используйте Railway CLI:
railway up
```

---

## 🔐 Проверка безопасности

### Что происходит БЕЗ вашего Telegram ID в конфигах:

**Текущая защита (только по username):**
- ✅ Работает, если username = `@atavaliev`
- ❌ **УЯЗВИМОСТЬ**: если кто-то изменит username на `@atavaliev` в Telegram, он получит доступ к админке

### Что происходит С вашим Telegram ID:

**Усиленная защита:**
- ✅ Проверка по **неизменяемому** Telegram ID (первичная)
- ✅ Проверка по username (запасная, если ID не найден)
- ✅ **БЕЗОПАСНО**: даже если кто-то украдёт username, ID не совпадёт → доступ запрещён

---

## 📊 Как пользоваться админ-панелью

### Вход в админку:

1. Откройте приложение в Telegram
2. Перейдите в **Профиль**
3. Прокрутите вниз → найдите **красную кнопку "Админ-панель"**
4. Нажмите → откроется dashboard с полной статистикой

**Если кнопки нет:**
- Проверьте, что ваш Telegram ID добавлен в конфиги (см. Шаг 2)
- Проверьте, что приложение пересобрано (см. Шаг 3)
- Проверьте, что новая версия задеплоена (см. Шаг 4)

---

## 📈 Что показывает админ-панель

### Секция 1: Live-статистика
- **Онлайн сейчас** — пользователи, активные последние 5 минут
- **Активны сегодня** — активные за последние 24 часа
- **Всего пользователей** — общее количество + активные эту неделю
- **Средняя сессия** — сколько времени проводят в приложении

### Секция 2: Популярные страницы
Топ-5 самых посещаемых страниц за последние 7 дней:
- Главная, Намазы, Коран, Викторина, и т.д.
- Показывает количество просмотров каждой

### Секция 3: Популярные действия
Топ-5 самых частых действий за последние 7 дней:
- "Отметил намаз", "Включил Коран", "Завершил викторину", и т.д.
- Показывает, какими фичами пользуются больше всего

### Секция 4: Средние показатели
- Средний уровень пользователей
- Средние очки
- Средний страйк (дни подряд)

### Секция 5: Таблица всех пользователей
- **Имя** + город + уровень
- **Очки** (сортировка по кликам на заголовок)
- **Намазы** (общее количество отмеченных)
- **Страйк** (дни подряд)
- **Поиск** по имени или городу
- **Пагинация** по 20 пользователей
- **Авто-обновление** каждые 30 секунд

---

## 🛡️ Что если кто-то попытается взломать админку?

### Защита на клиенте:
1. Кнопка "Админ-панель" **не показывается** неадминам
2. Route `/admin` **редиректит** на главную для неадминов

### Защита на сервере:
3. Endpoint `/api/admin/users` **возвращает 403 Forbidden** без правильных headers
4. Endpoint `/api/admin/analytics` **возвращает 403 Forbidden** без правильных headers
5. Headers проверяются: `X-Telegram-Id` и `X-Telegram-Username`
6. Telegram ID **не может быть подделан** — он приходит из Telegram WebApp context

### Вывод:
Даже если злоумышленник:
- Найдёт URL `/admin`
- Откроет Dev Tools и подделает username
- Попытается отправить запрос на `/api/admin/users`

Он **НЕ СМОЖЕТ** получить доступ, потому что:
- Telegram ID берётся из `window.Telegram.WebApp.initDataUnsafe.user.id`
- Это значение **нельзя подделать** — оно устанавливается Telegram при запуске WebApp
- Сервер проверяет, что ID есть в списке `ADMIN_TELEGRAM_IDS`

---

## 🔥 Что добавлять в будущем (если понадобится)

### При росте до 10,000+ пользователей:
1. **Авто-очистка старой аналитики** (старше 30 дней)
   ```sql
   DELETE FROM analytics WHERE timestamp < (strftime('%s', 'now', '-30 days') * 1000);
   ```
   Добавить cron job на сервере.

2. **Экспорт данных в CSV/Excel**
   Кнопка "Скачать отчёт" в админке.

3. **Графики по дням/неделям**
   Линейный график активности пользователей.

4. **Push-уведомления для админа**
   Когда онлайн > 100 пользователей или найдена аномалия.

---

## 📝 Итоговый чеклист перед деплоем

- [ ] Скопировал свой Telegram ID из Profile
- [ ] Добавил ID в `src/lib/adminConfig.ts` → массив `ADMIN_TELEGRAM_IDS`
- [ ] Добавил ID в `server.js` → массив `ADMIN_TELEGRAM_IDS` (строка ~68)
- [ ] Запустил `npm run build` — сборка прошла успешно
- [ ] Задеплоил на Railway (push to GitHub или `railway up`)
- [ ] Проверил, что кнопка "Админ-панель" видна в Profile
- [ ] Проверил, что админка открывается и показывает данные
- [ ] Проверил, что другие пользователи НЕ видят кнопку админки

---

## 🚨 Если что-то не работает

### Проблема 1: Кнопка "Админ-панель" не видна

**Решение:**
1. Откройте Dev Tools (F12) → Console
2. Введите: `getTelegramUser()`
3. Проверьте, что ваш `id` совпадает с тем, что добавили в конфиг
4. Проверьте, что файлы пересобраны (`npm run build`)

### Проблема 2: Админка показывает "Access denied"

**Решение:**
1. Откройте Dev Tools → Network
2. Найдите запрос `/api/admin/users`
3. Проверьте headers: должны быть `X-Telegram-Id` и `X-Telegram-Username`
4. Проверьте, что сервер перезапущен после изменения `server.js`

### Проблема 3: Онлайн = 0 (хотя вы в приложении)

**Решение:**
- Это нормально! "Онлайн" считаются пользователи, активные за последние **5 минут**
- События отправляются батчами каждые **10 секунд**
- Подождите 10-15 секунд → обновите админку (кнопка Refresh) → счётчик обновится

---

## 📞 Поддержка

Если возникли вопросы или нужна помощь:
- Telegram: @atavaliev
- Документация по масштабируемости: `SCALABILITY.md`

---

**Готово к запуску! 🚀**

Теперь вы можете приглашать пользователей и следить за статистикой в реальном времени.
