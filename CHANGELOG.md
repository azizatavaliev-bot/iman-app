# CHANGELOG — IMAN App

## 2026-07-20 (вечер, 6) — Telegram-бот: крутые фичи
- **`/hadith` — большой пул вместо 30 захардкоженных.** Теперь случайный хадис берётся из тех же ~16 300 (Бухари/Муслим/Абу Дауд/Сады праведных), что и в Mini App, с указанием степени достоверности. Fallback на старый короткий список при недоступности файлов.
- **`/ayat` — весь Коран (6236 аятов) вживую**, вместо 25 заранее заданных цитат. Живой запрос к alquran.cloud + русские названия сур (извлечены из `Quran.tsx` в `public/data/surah-names-ru.json`, 114 сур) — было "Сура «The Cave»", стало «Сура «Пещера»».
- **`/dua` — 93 дуа** вместо 15 (данные экспортированы из `src/data/dua.ts` в `public/data/dua-pool.json`).
- **🆕 `/audio` — реальная озвучка прямо в Telegram.** Присылает случайную аудиодорожку из библиотеки islamhouse.com (109 серий/384 дорожки — та же база, что в разделе `/audio` Mini App) через `sendAudio` (Telegram сам качает по прямой ссылке).
- **🆕 Реальные адхан-уведомления.** Раньше пуши шли по фиксированным часам (7:00/13:00/20:00...) независимо от намаза. Теперь бот раз в минуту сверяет текущее время с реальным расчётом (Aladhan API) и присылает «Наступило время намаза: X» ровно в момент каждого из 5 намазов (Фаджр/Зухр/Аср/Магриб/Иша), с кнопкой «Я прочитал намаз».
- Все новые фичи протестированы отдельными скриптами (пул хадисов — граничные индексы, дуа, аудио-каталог, живой аят, тайминг адхана с имитацией дрейфа опроса) — логика верна.
- Файлы: `server.js` (+загрузчики контента, +sendAudio, +адхан), `public/data/dua-pool.json`, `public/data/surah-names-ru.json` (новые).

## 2026-07-20 (вечер, 5) — Фикс наложения плавающих виджетов (таймер чтения + аудио-плеер)
- **Причина:** захардкоженные несогласованные отступы. Мини-плеер аудио стоял на `bottom: 60px`, но реальная высота нижней навигации — **83px** (панель + подпись «by...») → плеер на 23px налезал на навбар. Таймер чтения поднимался на `148px` при активном плеере — этого не хватало (реальная высота плеера 65px, нужно 83+65=148 без зазора) → таймер накладывался на плеер.
- **Фикс:** `AudioPlayer.tsx` мини-плеер → `bottom: 83px` (точно над навбаром, без наезда). `ReadingTimer.tsx` → `bottomOffset` 93px (без плеера) / 156px (с плеером, даёт чистый зазор 10px).
- Проверено расчётом+рендером реальных CSS-классов: зазор таймер↔плеер 10px, плеер садится вплотную над навбаром без наложения.

## 2026-07-20 (вечер, 4) — Вернул цветные иконки + анимацию (без свечения)
- По фидбеку вернул цветные градиентные фоны иконок (`grad`) по категориям и анимацию при наведении (`icon-fx-0..5`: bounce/spin/wiggle/pulse/swing/jelly) — как было изначально на проде.
- Свечение (colored box-shadow) и глянцевый блик **не возвращал** — только обычная `shadow-lg`, без цветного glow.

## 2026-07-20 (вечер, 3) — Упрощение блока функций (минимализм по фидбеку)
- Убраны цветные свечения и глянцевые блики у иконок (не понравились) — плитки плоские, единый приглушённый фон, только контур иконки цветной.
- Оставлена группировка по стеклянным панелям-секциям (это понравилось) + иконки в заголовках секций «Истории и сира»/«Развитие и прогресс».

## 2026-07-20 (вечер, 2) — Редизайн блока функций на главной
- **Секции меню (Коран и знания / Истории и сира / Развитие и прогресс / Ещё / Сообщество)** обёрнуты в стеклянные панели (`glass-card`) вместо голого грида на фоне страницы — визуально группируются.
- **Иконки плиток:** добавлен глянцевый блик сверху + цветное свечение (box-shadow) под акцентный цвет каждой иконки — вместо плоского флэт-цвета. *(откачено в следующей правке по фидбеку)*
- **Заголовки секций:** иконка в кружке с лёгким свечением под цвет секции + градиентная линия-разделитель. Добавлены недостающие иконки для «Истории и сира» (свиток) и «Развитие и прогресс» (кубок) — раньше падали на дефолтную звёздочку.
- Файл: `src/pages/Dashboard.tsx`.

## 2026-07-20 (вечер) — Редизайн иконки + аудит
- **Новая иконка приложения** (`favicon.svg`, `icons/icon-192.svg`, `icons/icon-512.svg`): было — плоский текст «IMAN» + эмодзи-полумесяц (криво рендерится на разных ОС). Стало — векторный полумесяц + звёзды (не эмодзи), глубокий изумрудный градиент + золото, в стиле сплэш-экрана. Единая viewBox, масштабируется под все размеры.
- **Аудит:** проверены вживую все ключевые экраны (главная, хадисы — 5 вкладок/16 361 хадис, аудио-библиотека — 109 серий/384 дорожки, 99 имён, кнопки «назад») — 0 ошибок. Убран неиспользуемый импорт `Headphones` в `App.tsx`.

## 2026-07-20 — Аудио-библиотека (проверенное аудио на русском) + замена робо-TTS
- **Новый раздел «Аудио»** (`/audio`, `src/pages/AudioLibrary.tsx`): **109 серий, 384 дорожки** проверенного религиозного аудио на русском с **islamhouse.com** (официальный da'wah-портал, реальные голоса — Кулиев, Мухаммад Басыр…). Категории: Коран (перевод), тафсир, хадисы, сира, вероубеждение, поклонение, дуа, лекции, хутбы.
- **Стрим напрямую** (MP3 с `d1.islamhouse.com`, range-запросы 206 → перемотка) — ничего не качаем, всегда актуально. Мини-плеер: играть/пауза, перемотка, длительность.
- Данные: `public/data/audio/catalog.json` (собрано из sitemap islamhouse). Вход — карточка на странице «Нашиды» (`Nasheeds.tsx`) + маршрут в `App.tsx`.
- **Коран: убран робо-голос (Web Speech TTS).** Кнопка «Слушать перевод» больше не читает синтезатором — ведёт в аудио-библиотеку с реальной озвучкой (`Quran.tsx`, удалён `speakTranslation`/`speechSynthesis`).

## 2026-07-19 22:30 — Коран/Намаз: достоверность, чтецы, режимы, локация
- **P0 Басмала (достоверность текста).** Аят 1 больше не содержит вшитую басмалу (кроме Аль-Фатихи и Ат-Тауба). `cleanAyahArabic` в `Quran.tsx` — арабский строго совпадает с переводом/транскрипцией. Проверено на сурах 1/2/9/112.
- **P0 Прогресс чтения.** Убрана авто-отметка суры прочитанной при открытии (мис-атрибуция). Прогресс растёт только по явной кнопке «Отметить прочитанной», привязанной к текущей суре (`Quran.tsx`).
- **P0 Хифз.** Новая сура стартует с 0% (было 50%). Самоисцеление старых записей с 0 повторений (`storage.ts`).
- **P0 Аудио.** Предзагрузка следующего аята → бесшовный переход, счётчик продвигается сразу (`Quran.tsx`).
- **Чтецы на русском (все ~240).** API mp3quran переключён на `language=ru`. Исправлен скрытый баг устаревших id (id 7 ≠ Аль-Афаси) → выбор популярного чтеца играл чужое аудио. Актуальные id + био (`reciters-ru.ts`, `api.ts`).
- **Режим «Книга» + таймер + позиция.** Скролл/книга с 3D-перелистыванием, память позиции, таймер-помидор чтения в Коране и Сире.
- **Верхний блок Корана.** Карточка «О суре» (значение, мекканская/мединская, значимость), «Слушать перевод» (русский TTS), чистая сетка управления.
- **P2 Локация/метод намаза.** Город на экране «Намаз» (обратный геокодинг Nominatim, `geocode.ts`); автометод **САДУМ** для СНГ при первом запуске (`prayer-methods.ts`, `Prayers.tsx`).
- **P2 Тосты.** Раздельные сообщения для намаза вовремя / с опозданием / все 5 (`Prayers.tsx`).
- **P2 Поиск сур.** Одиночная цифра «2» теперь фильтрует по номеру (точное/префиксное совпадение).
- **Сира.** Сверка с источниками (Ибн Хишам/Ибн Касир/Бухари): убрана ошибка «священный голубь», слабые предания помечены, фикс числа верблюдов и года рождения.

## 2026-07-19 21:50
- **Хадисы: +«Сады праведных» (Рийад ас-Салихин).** Добавлен 5-й сборник — классика достоверных хадисов для чтения по темам: **1885 хадисов, 363 главы** (макс № 1896, ~99% полноты), русский + арабский + степень достоверности, с русскими названиями глав. Источник перевода: isnad.link. Итого в приложении **~16 300 хадисов**, все локально.
- **Главная: фикс процента уровня.** Было `14.499999999999998%` → округлено (`Math.round`) в `Dashboard.tsx`.
- **Озвучка хадисов:** пробовали Web Speech (робо-голос) — по просьбе убрали, т.к. реальной человеческой озвучки перевода на русском для всех хадисов нет, а генерировать весь объём нереально.

## 2026-07-19 21:20
- **Хадисы → локальный бандл.** Переведены с внешнего CDN (`fawazahmed0/hadith-api`) на локальные файлы `public/data/hadiths/`. Теперь **14 436 хадисов** офлайн, без сетевых запросов: Бухари 7196, Муслим 2638, Абу Дауд 4602 (русский + арабский).
  - Фикс главного бага: русское издание Муслима на CDN было переведено лишь на 36% (хадис №1 пустой) → блок не раскрывался. Пустые отфильтрованы, Муслим открывается.
  - Добавлена вкладка **Абу Дауд** со степенями достоверности (сахих/хасан/да'иф по оценке Аль-Албани).
  - Русские названия книг вместо номеров («Вера», «Очищение»…), бейджи достоверности на карточках.
  - Загрузка по книге + кэш в памяти → быстрый старт и работа офлайн.
  - `src/lib/api.ts`: `getHadithSection` читает локально, добавлен `getHadithBookIndex`. `src/pages/Hadiths.tsx`: 4 сборника, оглавление, степени.
- **99 имён — фикс викторины.** Если у другого имени такой же перевод («Вечный» у Ас-Самад и Аль-Баки), появлялись 2 одинаковых варианта и засчитывался неверный. Совпадающие переводы исключены из пула (`src/pages/NamesGame.tsx`).
- **Навигация — убраны двойные кнопки «Назад».** Favorites, Habits, PrayerFlow, Wallpapers, ZikrCounter имели свою кнопку + глобальную. Добавлены в `PAGES_WITH_OWN_BACK` (`src/App.tsx`).
